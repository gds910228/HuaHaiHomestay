# 需求 Spec · {变更名称}

> 阶段1 产出。用 brainstorming skill 苏格拉底式澄清后填写。

## 一、需求理解

### 用户原始诉求
(原话记录)

### 澄清后理解
(用自己的话复述,确认与用户一致)

## 二、验收标准(Acceptance Criteria)

逐条可验证,每条标注验证方式(测试/截图/手动走查):

1. (标准) -- 验证:
2. (标准) -- 验证:
3. ...

## 三、影响范围

- 触及页面:
- 触及云函数 + event.type:
- 触及集合 + 字段:
- 触及触发器:无 / 有
- 触及设计系统 token:无 / 有

## 四、合规与权限自检(阶段2 评审重点)

- [ ] 文案无"游玩路线/旅行社/预订/下单/支付"等敏感词(编码规范§9)
- [ ] 定位用 wx.getFuzzyLocation,非 getLocation(编码规范§3)
- [ ] 新集合用 ensureCollection(编码规范§4)
- [ ] 密码/KEY 走 env var(编码规范§5)
- [ ] 价格字段 fixedPrice 兼容旧 price{low,high}(编码规范§1)
- [ ] 不假设旧数据有新字段(用 ?? 兼容)

## 五、待决议项(交用户,HITL-1)

(列出需要用户拍板的开放问题,无则填"无")

## 六、技术方案概要

(简述实现思路,详细 task 拆解见同目录 `tasks.md`)
