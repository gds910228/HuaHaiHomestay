# 部署报告 · {变更名称}

> 阶段9 产出。按开发流程规范§3 部署。部署 ≠ 生效,必须回查。

## HITL-3 部署参数确认(部署前)

交用户确认:
- [ ] 部署哪些云函数:
- [ ] 是否涉及定时触发器变更:否 / 是(云函数+cron):
- [ ] 是否需要跑 init-database:否 / 是(type):
- [ ] 是否需要云控制台手动操作(建集合/配触发器):否 / 是:

用户确认后部署。

## 部署执行

| 云函数 | 部署方式 | 部署时间 | 结果 |
|---|---|---|---|
| | 开发者工具上传 / 脚本 | | ✅/❌ |

## 部署后验证

- [ ] 云端日志:启动无错
- [ ] 前端调用该 event.type:返回 success:true
- [ ] 触发器(若有):云控制台可见且生效
- [ ] 真机走查关键路径:正常

## 触发器核查

- 涉及的 triggers 是否已落 config.json:
- 部署后云控制台是否同步:
(不符则记录漂移,补进编码规范§8)

## 回滚记录(如发生)

- (无 / 回滚操作 + 原因 + 回退到阶段几)
